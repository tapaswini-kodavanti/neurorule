# Dependencies for ESP service.

keras==3.8.0
tensorflow==2.16.1
h5py>=3.10.0
numpy>=1.26.4,<2.0
scipy>=1.12.0

# Click is a BSD licensed CLI library documentation found at, https://palletsprojects.com/p/click/
click
s3fs>=2024.2.0

# For gRPC
# Note: We cannot yet let these float upwards because if we do, they will get different
#       versions and the tests needs these versions to be in sync in order for them to pass.
#       Maybe when keras and tensorflow float, these can too.
grpcio==1.62.0
grpcio-tools==1.62.0
grpcio-health-checking==1.62.0
protobuf>=4.25.3
ruamel.yaml>=0.18.6

# Schema validation
jsonschema>=4.21.1
